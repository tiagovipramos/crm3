# Configuração do Servidor
PORT=3001
NODE_ENV=development

# Banco de Dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=protecar_crm
DB_USER=root
DB_PASSWORD=root123

# JWT
JWT_SECRET=change-this-secret-key-in-production-d89f7a6b5c4e3a2f1d0c9b8a7e6d5c4b3a2f1e0d
JWT_EXPIRES_IN=24h  # Reduzido de 7d para 24h por segurança

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:3000

# IMPORTANTE:
# 1. Copie este arquivo para .env: cp .env.example .env (Linux/Mac) ou copy .env.example .env (Windows)
# 2. Altere DB_PASSWORD e JWT_SECRET em produção
# 3. O arquivo .env não deve ser commitado (já está no .gitignore)
# 4. Este projeto usa MySQL 8.0
